<Page
    x:Class="UnoOnnxSamples.Views.PageTwo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnoOnnxSamples.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="3*"/>
			<RowDefinition Height="*" />
			<RowDefinition Height="6*" />
		</Grid.RowDefinitions>

		<Image x:Name="BasicImage"
			   HorizontalAlignment="Stretch"
			   VerticalAlignment="Stretch"
			   Source="ms-appx:///Content/PyTorch/fishh.jpeg"/>

		<Button x:Name="RunButton"
				Grid.Row="1"
				Content="Run Super Resolution Reconstruction"
				HorizontalAlignment="Stretch"
				VerticalAlignment="Center"
				Click="RunButton_Click"/>

		<Image x:Name="ResultantImage"
			   HorizontalAlignment="Stretch"
			   VerticalAlignment="Stretch"/>
	</Grid>
</Page>
